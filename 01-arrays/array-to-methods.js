const heroes = ['Batman', 'Superman', 'Flash', 'Aquaman',];
// heroes.sort();

// const deletedHeroes = heroes.toSpliced(0, 2, 'Green Latern');

const deletedHeroes = heroes.toSpliced(0, 2, 'Green');



// const heroesCopy = heroes;

// const sortedHeros = heroes.toSorted();
// heroes.reverse();

// const reversedHeroes = heroes.toReversed();



console.table(heroes);
console.table(deletedHeroes);
// console.table(reversedHeroes);